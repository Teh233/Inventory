import React from "react";
import { Box, styled } from "@mui/material";
const Style = styled("div")(({ theme }) => ({}));

const Footer = () => {
  return (
    <Box bgcolor="grey" p={2}>
      Footer
    </Box>
  );
};

export default Footer;
